"""Platform-wide constants (not case-specific)."""

SLOT_ORDER = ("agent_1", "agent_2", "agent_3", "agent_4")
DEFAULT_CASE_ID = "sao-paulo-dropout"
SUPPORTED_LANGUAGES = ("en", "pt", "fi")

# Shared invite link: one link, many separate guest answers (append storage)
MAX_HUMAN_ANSWERS_PER_SESSION = 100
DEFAULT_INVITE_MAX_RESPONSES = 100
