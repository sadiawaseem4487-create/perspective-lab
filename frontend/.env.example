# When UI is hosted on Vercel (API stays on Render):
# VITE_API_BASE_URL=https://perspective-lab.onrender.com
#
# Leave empty for same-origin (Render Docker or local FastAPI serving frontend/dist).
# On *.vercel.app the UI auto-targets https://perspective-lab.onrender.com if unset.
